[package]
name = "mobile"
version = "0.1.0"
edition = "2021"
authors = ["CharaTech"]
description = "Mobile application security analysis: Android APK, iOS IPA"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
chrono = "0.4"
regex = "1.10"
zip = "0.6"
plist = "1.6"

[dev-dependencies]
tempfile = "3.10"
